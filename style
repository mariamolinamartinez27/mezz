/* Paleta de colores */
:root {
    --color-principal-verde: #1a431a; /* Verde oscuro */
    --color-acento-rojo: #962828; /* Rojo */
    --color-fondo-claro: #f5f5dc; /* Crudo */
    --color-texto-oscuro: #121212; /* Negro casi puro */
    --color-texto-claro: #f5f5f5; /* Blanco casi puro */
}

/* Estilos generales */
body {
    font-family: 'Helvetica Neue', Arial, sans-serif;
    margin: 0;
    background-color: var(--color-fondo-claro);
    color: var(--color-texto-oscuro);
    line-height: 1.6;
}

/* Estilos de tipografía */
h1, h2, h3 {
    font-family: 'Georgia', serif;
    color: var(--color-principal-verde);
    text-align: center;
    margin-bottom: 0.5rem;
}

h1 {
    font-size: 3rem;
    color: var(--color-acento-rojo);
}

/* Estilos del header */
header {
    background-color: var(--color-principal-verde);
    color: var(--color-texto-claro);
    padding: 4rem 0;
    text-align: center;
}

header p {
    font-size: 1.2rem;
    font-style: italic;
    color: var(--color-texto-claro);
}

/* Estilos de las secciones principales */
main {
    max-width: 900px;
    margin: 2rem auto;
    padding: 0 1rem;
}

section {
    padding: 2rem 0;
    border-bottom: 1px solid var(--color-principal-verde);
}

/* Estilos de los proyectos */
.proyecto {
    background-color: var(--color-fondo-claro);
    border: 1px solid var(--color-principal-verde);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
}

.proyecto a {
    color: var(--color-acento-rojo);
    text-decoration: none;
    font-weight: bold;
}

.proyecto a:hover {
    text-decoration: underline;
}

/* Estilos de los enlaces de contacto */
#contacto ul {
    list-style-type: none;
    padding: 0;
    text-align: center;
}

#contacto li {
    display: inline-block;
    margin: 0 1rem;
}

#contacto a {
    color: var(--color-principal-verde);
    text-decoration: none;
    font-weight: bold;
}

#contacto a:hover {
    color: var(--color-acento-rojo);
    text-decoration: underline;
}

/* Estilos del footer */
footer {
    background-color: var(--color-texto-oscuro);
    color: var(--color-texto-claro);
    text-align: center;
    padding: 1.5rem 0;
    margin-top: 2rem;
}
